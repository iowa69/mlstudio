"""Profile storage, distance matrix, MST construction.

- store.py     — SQLite schema and access layer.
- distance.py  — Hamming distance over allele profiles (vectorized).
- mst.py       — Minimum spanning tree (goeBURST / Prim) + cluster assignment.

Stub — M5.
"""
